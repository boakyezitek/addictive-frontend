<?php

namespace App\Console;

use Illuminate\Console\Scheduling\Schedule;
use Illuminate\Foundation\Console\Kernel as ConsoleKernel;

class Kernel extends ConsoleKernel
{
    /**
     * The Artisan commands provided by your application.
     *
     * @var array
     */
    protected $commands = [
        //
    ];

    /**
     * Define the application's command schedule.
     *
     * @param  \Illuminate\Console\Scheduling\Schedule  $schedule
     * @return void
     */
    protected function schedule(Schedule $schedule)
    {
        $schedule->command('backup:clean')->monthly()->at('01:00')->environments('production');
        $schedule->command('backup:run')->monthly()->at('02:00')->environments('production');
        $schedule->command('telescope:prune --hours=48')->daily();
        $schedule->command('subscription:expired')->everyThirtyMinutes()->environments(['staging', 'development']);
        $schedule->command('subscription:expired')->daily()->environments('production');
    }

    /**
     * Register the commands for the application.
     *
     * @return void
     */
    protected function commands()
    {
        $this->load(__DIR__.'/Commands');

        require base_path('routes/console.php');
    }
}
